source 'https://rubygems.org'

ruby '2.0.0'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.0.3'

# Use pg or sqlite3 as the database for Active Record depending on production or development
group :production do
  gem 'pg'
end

group :development do
  gem 'sqlite3'
end

group :development, :test do
  gem 'rspec-rails'
  gem 'factory_girl_rails', '~> 4.0'
  gem 'database_cleaner'
end

group :assets do
  gem 'bootstrap-sass', '~> 3.1.1'
end


gem 'sass-rails', '~> 4.0.0'
gem 'uglifier', '>= 1.3.0'
gem 'coffee-rails', '~> 4.0.0'
gem 'turbolinks'

gem 'jbuilder', '~> 1.2'

group :doc do
  gem 'sdoc', require: false
end

gem 'bootstrap-sass', '~> 3.1.1'
gem 'faker'
gem 'devise'
gem 'pundit'

gem 'jquery-rails'

gem 'rails_12factor', group: :production

gem 'redcarpet'
gem 'carrierwave'
gem 'mini_magick'
gem 'fog'
gem 'figaro'
gem 'will_paginate', '~> 3.0.5'
gem 'omniauth-facebook'
